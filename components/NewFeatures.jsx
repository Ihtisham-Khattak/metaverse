import styles from "../styles";

const NewFeatures = ({ title, imgUrl, subtitle }) => (
  <div className="flex flex-1 flex-col sm:mx-w-[250px] min-w-[210px]">
    
    New Features
  </div>
);

export default NewFeatures;
